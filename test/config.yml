title: "Web Server"
app_title: "WebServer"
desc: "A Web Server."
icon: "/assets/icon.png"

public_uri: "/public/"
csp: no

DebugMode: yes

port_http: 8080
port_ssl: 8443

origins: [
  "localhost",
  # "example.com",
]

proxies: [
  "127.0.0.1",
  # "192.168.0.1",
]

Vars:
  myvar: "example value"
